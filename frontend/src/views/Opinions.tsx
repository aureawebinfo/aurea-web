import Section from "@/components/Section";
import SectionContent from "@/components/SectionContent";
import SectionHeader from "@/components/SectionHeader";

export default function Opinions() {
    return (
        <Section>
            <SectionHeader title="Secion de opiniones" subtitle="esta es la seccion de opiniones de los clientes con los que hemos trabajado" />
            <SectionContent>
                <p>cartas donde van las opiniones</p>
            </SectionContent>
        </Section>
    )
}